# Other implementations

Non–JavaScript implementations of the MiniASM VM and tools.

- **python/** — CLI interpreter and exercise runner (`config.py`, `interpreter.py`, `exercises.py`). Run from this directory: `python exercises.py`.
- **c/** — C interpreter (`interpreter.c`).

The main JS/Web implementation lives at the repository root (`js/`, `index.html`, etc.).
